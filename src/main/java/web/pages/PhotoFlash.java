package web.pages;

import org.openqa.selenium.WebDriver;

public class PhotoFlash {
    private final WebDriver driver;

    public PhotoFlash(WebDriver driver) {
        this.driver = driver;
    }

}
